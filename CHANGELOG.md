# 更新记录

本项目的应用版本以 `package.json` 为准。Git 发布标签为 `v<应用版本>`，Docker Hub
正式标签为 `<应用版本>`；生产环境使用经验证的镜像 digest 固定版本。

## 1.0.15-ns.5 — 2026-09-18

- 单镜像内置并监管 Moon 家长控制采集服务，容器健康检查同时覆盖 Web 和 Moon。
- Moon 私钥、加密授权及 Nintendo Store 授权和低频调度状态都固定保存在 `/data` 持久卷；新实例分别授权，不含个人凭据。
- Docker Compose 要求显式指定个人 NS2 镜像，避免误拉不含游玩功能的上游镜像。

## 1.0.15-ns.4 — 2026-09-18

- 设置页增加运行版本与构建提交，便于核对实际部署。
- 发布门禁检查应用、锁文件与 Git 标签的版本一致；镜像带版本和提交标签。

## 1.0.15-ns.3 — 2026-09-18

- Docker 发布改为原生 amd64 与 arm64 构建，再合并多架构镜像。
- 保留每个架构的中间标签，并为构建设置超时和正式镜像的平台检查。

## 1.0.15-ns.2 — 2026-09-17

- 修复旧版 Moon v3 数据库迁移；该标签的多架构构建未完成，不作为可部署镜像。

## 1.0.15-ns.1 — 2026-09-17

- 首次个人版镜像；与 OVH 的旧版 Moon v3 数据库不兼容，不可用于该环境。
